# Role: 资深物流算法工程师 & 交互可视化专家# Context我需要开发一款“车辆配载与3D可视化装箱系统”。该系统需处理经典的 3D Bin Packing 问题，并带有严格的物流行业约束条件。请根据以下需求，输出完整的 Python 代码（包括后端算法逻辑与前端可视化界面）。# Tech Stack- 后端/算法：Python, Pandas- 前端/UI：Streamlit (Web交互界面)- 可视化：Plotly (3D交互式图表) + Matplotlib (2D三视图)- 文件处理：Openpyxl (读写Excel)# Data Structures (数据输入规范与模版要求)系统必须提供“下载导入模板”功能，生成并导出以下两个 Excel 模板供用户填写：1. 【货物数据源 (Cargo)】：   - Cargo_ID (货物编号)   - Length, Width, Height (长、宽、高 cm)   - Gross_Weight (单件毛重 kg)   - Fixed_Bottom (是否固定底面：是/否)   - Specify_Vehicle_Type (指定车型：箱车/飞翼车/平板车/高栏车/不指定)   - Unload_Order (卸货顺序：数字越小越先卸货，用于LIFO)   - Max_Stacking_Layers (最大可叠层数)   - Quantity (数量)2. 【车型数据源 (Vehicle)】：   - Vehicle_Category (车型：箱车/飞翼车/平板车/高栏车)   - Internal_Length, Internal_Width, Internal_Height (车厢内径长宽高 cm)   - Max_Payload (最大载重量 kg)# Core Logic & Constraints (核心算法约束)请在算法中严格实现以下装载逻辑：1. **尺寸膨胀与缓冲 (Buffer 变量，需在代码前部定义为易修改的全局变量)**：   - 若货物的 `Fixed_Bottom` == '是'，则该货物的长宽高各增加 1.0 cm 的 buffer。   - 若货物的 `Fixed_Bottom` == '否'，则该货物的长宽高各增加 1.5 cm 的 buffer。2. **旋转约束 (Rotation)**：   - 若 `Fixed_Bottom` == '是'，仅允许水平旋转（长宽互换，高度 Z 轴绝对固定）。   - 若 `Fixed_Bottom` == '否'，允许 3D 空间内的 6 个方向全量旋转。3. **重量与载重限制**：   - 单车装载货物的总毛重绝不能超过该车型的 `Max_Payload`。4. **堆叠物理规则 (Stacking & Bearing)**：   - 重不压轻：上层货物的重量必须 <= 下层货物的重量。   - 面积支撑：上层货物的底面积，不能超过其正下方支撑货物底面积的 4/3（即允许微小的外延，但受严格限制）。5. **车型匹配与高度豁免**：   - 最终输出的匹配车型名称需拼接为如 "9.6m 箱车"、"13.7m 平板车" 的格式。   - 若匹配到“平板车”或“高栏车”，算法在 Z 轴（高度）的校验约束解除，只需满足 X 和 Y 的边界限制。6. **LIFO 卸货顺序约束**：   - 检查货物的 `Specify_Vehicle_Type`。若为“箱车”，则在装箱算法中必须启用 LIFO（后进先出）逻辑：`Unload_Order` 数字小的（先卸货）必须装在靠近车厢尾部（车门）的位置，`Unload_Order` 数字大的（后卸货）装在车厢深处。其余车型忽略此顺序限制。# Visualization & UI Requirements1. **控制面板**：提供“下载 Excel 模板”按钮、数据上传组件，以及计算执行按钮。2. **3D 交互视图**：   - 渲染车厢边框轮廓（半透明）。   - 按计算出的坐标渲染每一个货物，不同属性货物用不同颜色区分，表面标明 `Cargo_ID`。   - 支持鼠标拖拽旋转、缩放。3. **2D 三视图导出**：   - 生成标准的工程三视图（俯视图 Top、正视图 Front、侧视图 Side），指导现场装车。# Initialization请输出完整的单文件 `app.py` 代码（或合理的模块化代码）。代码需包含详尽的中文注释，特别是 3D-BPP 算法部分的逻辑说明。